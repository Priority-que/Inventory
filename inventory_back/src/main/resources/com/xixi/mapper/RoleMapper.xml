<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xixi.mapper.RoleMapper">
    <select id="getRoleCodesByUserId" resultType="string">
        select r.code
        from user_role ur inner join role r on ur.role_id = r.id
        where ur.user_id = #{userId} and r.deleted = 0 and r.status = 'ENABLED'
    </select>
</mapper>