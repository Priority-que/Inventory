spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://114.132.43.247:3306/inventory?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Ly0628..
  data:
    redis:
      host: 114.132.43.247
      port: 6379
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          max-wait: -1ms
          min-idle: 0
        shutdown-timeout: 100ms
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
      embedding:
        enabled: true
        options:
          model: text-embedding-v3
    vectorstore:
      redis:
        index-name: "ikun-rag-index:"
        prefix: "ikun-rag-prefix:"
        initialize-schema: true

security:
  jwt:
    secret: Always believe that something wonderful is about to happen.
    expire-minutes: 120
    header: Authorization
    prefix: Bearer
    blacklist-prefix: "auth:blacklist:"
mybatis-plus:
  mapper-locations: classpath*:com/xixi/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
